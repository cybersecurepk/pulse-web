import { RolesPermissionsView } from "@/feature/admin/settings/view";

export const metadata = { title: `Roles & Permissions` };

export default function Page() {
  return <RolesPermissionsView />;
}
