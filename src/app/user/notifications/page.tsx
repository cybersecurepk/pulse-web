import { UserNotificationsView } from "@/feature/user/notifications/view";

export const metadata = { title: `Notifications` };

export default function Page() {
  return <UserNotificationsView />;
}
